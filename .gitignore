# Local workspace pointing at a go-iroh checkout. Each machine's is its own;
# the module builds against the tagged version without it.
go.work
go.work.sum

# Build output from the Rust interoperability harness.
interop/target/
